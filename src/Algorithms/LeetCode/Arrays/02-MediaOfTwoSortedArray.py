# given two sorted arrays num1 and num2 of size of m and n, return the median of the two
# sorted arrays

# Follow up: the overall run time complexity should be O(log(m + n))

class Solution:
    def media_of_2_sorted_array(self, num1 : list[int], num2 : list[int]) -> int:
        pass